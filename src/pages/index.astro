---
import { getCollection } from "astro:content";
import { getRelativeLocaleUrl } from "astro:i18n";
import IconArrowRight from "@/assets/icons/IconArrowRight.svg";
import Layout from "@/layouts/Layout.astro";
import Header from "@/components/Header.astro";
import Footer from "@/components/Footer.astro";
import Card from "@/components/Card.astro";
import FormatBadge from "@/components/FormatBadge.astro";
import LinkButton from "@/components/LinkButton.astro";
import { getPostUrl } from "@/utils/getPostPaths";
import { getSortedPosts } from "@/utils/getSortedPosts";
import { SERVICE_CTA } from "@/data/knowledge";
import { CATEGORIES } from "@/data/categories";
import { CONTENT_FORMATS, SEARCH_QUERIES, TOPIC_CLUSTERS } from "@/data/contentSystem";
import config from "@/config";

const locale = Astro.currentLocale ?? config.site.lang;
const posts = await getCollection("posts", ({ data }) => !data.draft);
const sortedPosts = getSortedPosts(posts);
const homePath = getRelativeLocaleUrl(locale, "");

const byType = (type: string, limit = 4) =>
  sortedPosts.filter(post => post.data.contentType === type).slice(0, limit);

const latestPosts = sortedPosts.slice(0, 5);
const viralNotes = byType("viral_note", 4);
const fragments = byType("psychological_fragment", 6);
const deepAnalysis = byType("deep_analysis", 4);
const pillarArticles = byType("pillar_article", 4);
const popularCategories = CATEGORIES.filter(category =>
  [
    "relationships",
    "manipulation",
    "jealousy",
    "money-status",
    "emotional-dependence",
    "silence-ghosting",
  ].includes(category.slug)
);
---

<Layout
  title="Людський механізм — психологія стосунків, грошей, сексу і маніпуляцій"
  description="Масове психологічне медіа про стосунки, ревнощі, зради, маніпуляції, токсичних людей, гроші, секс, конфлікти і приховані мотиви поведінки."
>
  <Header />
  <main
    id="main-content"
    data-layout="index"
    data-home-path={homePath}
    class="app-layout"
  >
    <section id="hero" class="relative overflow-hidden border-border border-b pt-16 pb-14 sm:pt-24 sm:pb-20">
      <div class="relative z-10 grid gap-12 lg:grid-cols-[minmax(0,1fr)_minmax(18rem,0.72fr)] lg:items-center">
        <div class="max-w-3xl">
          <p class="section-kicker">Психологія повсякденних драм</p>

          <h1 class="mt-7 text-5xl leading-[1.05] font-semibold tracking-normal sm:text-7xl">
            Людський механізм
          </h1>

          <p class="mt-8 max-w-2xl text-lg leading-8 text-muted-foreground sm:text-xl sm:leading-9">
            Медіа про те, чому люди зникають, ревнують, маніпулюють, мовчать,
            закохуються не в тих, бояться грошей, втрачають бажання і воюють за
            любов, статус та контроль.
          </p>

          <div class="mt-10 flex flex-wrap gap-3 sm:mt-12">
            <LinkButton
              href={getRelativeLocaleUrl(locale, "posts")}
              class="bg-accent text-accent-foreground px-5 py-3 hover:opacity-90"
            >
              Читати розбори
              <IconArrowRight class="inline-block rtl:-rotate-180" />
            </LinkButton>
            <LinkButton
              href={getRelativeLocaleUrl(locale, "categories")}
              class="border-border border px-5 py-3 hover:text-accent"
            >
              Карта тем
            </LinkButton>
          </div>
        </div>

        <div class="human-hero-mark hidden lg:block" aria-hidden="true">
          <span class="human-hero-heart">М</span>
          <span class="human-hero-line"></span>
          <span class="human-hero-note">без відповіді</span>
        </div>
      </div>
    </section>

    <section id="search-queries" class="border-border border-b py-14 sm:py-20">
      <div class="max-w-2xl">
        <p class="section-kicker">Пошукові входи</p>
        <h2 class="section-title">Люди найчастіше шукають</h2>
      </div>

      <ul class="mt-8 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">
        {SEARCH_QUERIES.map(query => (
          <li>
            <a href={query.href} class="query-card block min-h-full">
              <span class="query-card-mark" aria-hidden="true"></span>
              <strong>{query.question}</strong>
              <span>Перейти до теми</span>
            </a>
          </li>
        ))}
      </ul>
    </section>

    <section id="latest-research" class="content-lane border-border border-b py-14 sm:py-20">
      <div class="lane-header">
        <div>
          <p class="section-kicker">Останні публікації</p>
          <h2 class="section-title">Нові розбори</h2>
        </div>
        <LinkButton href={getRelativeLocaleUrl(locale, "posts")}>
          Усі статті
          <IconArrowRight class="inline-block rtl:-rotate-180" />
        </LinkButton>
      </div>
      <ul class="mt-4">
        {latestPosts.map(post => <Card variant="h3" {...post} />)}
      </ul>
    </section>

    <section id="viral-notes" class="content-lane border-border border-b py-14 sm:py-20">
      <div class="lane-header">
        <div>
          <FormatBadge contentType="viral_note" />
          <h2 class="section-title mt-3">Viral Notes</h2>
          <p class="section-copy">{CONTENT_FORMATS.viral_note.description}</p>
        </div>
      </div>
      <ul class="mt-4">
        {viralNotes.map(post => <Card variant="h3" {...post} />)}
      </ul>
    </section>

    <section id="fragments" class="border-border border-b py-14 sm:py-20">
      <div class="lane-header">
        <div>
          <FormatBadge contentType="psychological_fragment" />
          <h2 class="section-title mt-3">Psychological Fragments</h2>
          <p class="section-copy">{CONTENT_FORMATS.psychological_fragment.description}</p>
        </div>
      </div>
      <ul class="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
        {fragments.map(post => (
          <li>
            <a href={getPostUrl(post.id, post.filePath, locale)} class="fragment-card block min-h-full">
              <span class="fragment-card-format">Fragment</span>
              <strong>{post.data.title}</strong>
              <span>{post.data.description}</span>
            </a>
          </li>
        ))}
      </ul>
    </section>

    <section id="popular-topics" class="border-border border-b py-14 sm:py-20">
      <div class="max-w-2xl">
        <p class="section-kicker">Topic clusters</p>
        <h2 class="section-title">Популярні теми</h2>
        <p class="section-copy">
          Кластери для внутрішньої перелінковки, SEO і AI search: кожна тема веде
          до хабів, категорій і серій матеріалів.
        </p>
      </div>

      <ul class="mt-8 grid gap-4 sm:grid-cols-2">
        {TOPIC_CLUSTERS.map(cluster => (
          <li>
            <a href={cluster.href} class="topic-cluster-card block min-h-full">
              <strong>{cluster.title}</strong>
              <span>{cluster.description}</span>
            </a>
          </li>
        ))}
      </ul>

      <ul class="mt-8 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">
        {popularCategories.map(category => (
          <li>
            <a href={getRelativeLocaleUrl(locale, `categories/${category.slug}`)} class="topic-chip block">
              {category.title}
            </a>
          </li>
        ))}
      </ul>
    </section>

    <section id="deep-analysis" class="content-lane border-border border-b py-14 sm:py-20">
      <div class="lane-header">
        <div>
          <FormatBadge contentType="deep_analysis" />
          <h2 class="section-title mt-3">Deep Analysis</h2>
          <p class="section-copy">{CONTENT_FORMATS.deep_analysis.description}</p>
        </div>
      </div>
      <ul class="mt-4">
        {deepAnalysis.map(post => <Card variant="h3" {...post} />)}
      </ul>
    </section>

    <section id="pillar-articles" class="content-lane border-border border-b py-14 sm:py-20">
      <div class="lane-header">
        <div>
          <FormatBadge contentType="pillar_article" />
          <h2 class="section-title mt-3">Pillar Articles</h2>
          <p class="section-copy">{CONTENT_FORMATS.pillar_article.description}</p>
        </div>
        <LinkButton href={getRelativeLocaleUrl(locale, "topics")}>
          Тематичні хаби
        </LinkButton>
      </div>
      <ul class="mt-4">
        {pillarArticles.map(post => <Card variant="h3" {...post} />)}
      </ul>
    </section>

    <section id="services" class="border-border border-b py-14 sm:py-20">
      <div class="max-w-2xl">
        <p class="section-kicker">Практика</p>
        <h2 class="section-title">Практичні формати</h2>
        <p class="section-copy">
          Майбутні продукти, які можна м'яко підключати до матеріалів без
          агресивної реклами.
        </p>
      </div>
      <ul class="mt-8 grid gap-5 sm:grid-cols-2">
        {SERVICE_CTA.map(service => (
          <li class="map-card border-border bg-muted/20 border p-5 sm:p-6">
            <span class="topic-sigil" aria-hidden="true"></span>
            <h3 class="mt-4 text-lg font-semibold">{service.title}</h3>
            <p class="mt-3 text-sm leading-7 text-muted-foreground">
              {service.description}
            </p>
          </li>
        ))}
      </ul>
    </section>
  </main>
  <Footer />
</Layout>

<script>
  document.addEventListener("astro:page-load", () => {
    const indexLayout = (document.querySelector("#main-content") as HTMLElement)
      ?.dataset;
    const baseRoot = import.meta.env.BASE_URL.replace(/\/?$/, "/");
    if (indexLayout?.layout) {
      sessionStorage.setItem("backUrl", indexLayout.homePath ?? baseRoot);
    }
  });
</script>
